
<div class="container">
<div class="row">
<%= render 'books/list', user: @user %>
<div class="col-lg-8">
  
<h2>Users</h2>
<table class="table table-hover table-inverse">
 <% @books.each do |book| %>

<thead>
     <tr>
      <th>image</th>
      <th>Opinion</th>
      <th></th>
      <th colspan="3"></th>
     </tr>
    </thead>

  <tr>
  <td><%= image_tag user_rget_profile_image %></td>
  <td><%= user.name %></td>
  <td>
  <%= link_to 'Show', book_path(book.id) %></tb>
  </tr>

  <% end %>
 </table>

</div>
</div>
</div>
