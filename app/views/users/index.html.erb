<div class="container">
<div class="row">
<%= render 'books/list', user: @user %>
<div class="col-lg-8">

<h2>Users</h2>
<table class="table table-hover table-inverse">
 

<thead>
     <tr>
      <th>image</th>
      <th>name</th>
      <th></th>
      <th colspan="3"></th>
     </tr>
    </thead>
<% @users.each do |user| %>
  <tr>
  <td><%= image_tag user.get_profile_image %></td>
  <td><%= user.name %></td>
  <td>
  <%= link_to 'Show', user_path(user) %></tb>
  </tr>

  <% end %>
 </table>

</div>
</div>
</div>
